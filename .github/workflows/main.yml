on: [push]
jobs:
  first-one:
    runs-on: ubuntu-latest
    steps:
    - name: Install Python
      run: sudo apt install python3
    - name: PWD
      run: pwd
    - name: Run tests
      run: python3 python/dsp_lab/src/signal.py -v
    
    
